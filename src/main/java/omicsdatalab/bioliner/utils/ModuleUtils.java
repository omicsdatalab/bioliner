package omicsdatalab.bioliner.utils;

import java.util.logging.Logger;

/**
 * This class contains utility methods related to Modules. Currently empty but may be added in the future.
 */
public class ModuleUtils {

    private static final Logger LOGGER = Logger.getLogger(ModuleUtils.class.getName() );
}
